services:
  api:
    build: .
    image: crud-list-api-server
    ports:
      - "3000:3000"
    env_file:
      - .env.docker
    volumes:
      - .:/app
      - /app/node_modules
    command: npx nodemon -L index.js